export enum StatusType {
    ONLINE = "online",
    DND = "dnd",
    IDLE = "idle",
    OFFLINE = "offline",
    STREAMING = "streaming"
}

export enum BackgroundType {
    IMAGE = "image",
    COLOR = "color"
}

export enum ProgressBarType {
    COLOR = "color",
    GRADIENT = "gradient"
}
